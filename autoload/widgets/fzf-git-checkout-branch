#!/bin/zsh

if ! git rev-parse --is-inside-work-tree >/dev/null 2>&1; then
  if [[ -n $TMUX ]]; then
    local msg='fzf-git-checkout-branch: Not a git repository'
    tmux split-window "less <<< \"$msg\""
  fi
  return 1
fi

__fzf::widget::init 'git checkout'

git branch --list -v | \
  grep -v '^\*' | \
  sed 's/^  *//' | \
  __fzf::widget::select $FZF_WIDGETS_OPTS[git-checkout-branch] +m

__fzf::widget::filter "awk '{print \$1}'"

__fzf::widget::insert
