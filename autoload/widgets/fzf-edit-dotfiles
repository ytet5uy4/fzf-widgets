#!/bin/zsh

selector-init "$EDITOR --"

private dir="$DCTL_PATH"

find $dir/.* -type f -o -type l | \
  sed "s|$dir/||g" | \
  selector-select -m

sed -i -e "s|^|$dir/|g" /tmp/selector

selector-exec
